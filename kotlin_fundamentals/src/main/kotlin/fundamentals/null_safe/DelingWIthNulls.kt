package section_01_fundamentals.fundamentals.null_safe

fun main() {
    // safe call operator
    // use the safe call operator to invoke functions safely on it

    // 1) val length = name?.length

    // Elvis operator
    // return a default vale if null

    //2) val length = name?.length ?: 0

    // NOt null assertions
    //
}